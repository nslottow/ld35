<!DOCTYPE html>
<html lang="en">

    <head>
        <link rel="icon" href="favicon.png"/>
        <meta charset="utf-8">
		<meta name="viewport" content="user-scalable=no">
		<meta name="apple-mobile-web-app-capable" content="yes">
		<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
		<meta name="mobile-web-app-capable" content="yes">

        <style>
			body, html {
				position: fixed;
				height: 100%;
			}
			body {
				overflow: hidden;
				margin: 0;
				padding: 0;
				background: #111111;
			}
		</style>

        {{#each project.app.web.libs~}}
            <script type="text/javascript" src="{{this}}"></script>
        {{/each}}
    </head>

    <body>
		{{#if project.app.web.debug_toolbar '!==' false}}
		<div style="width:100%;background-color:#999999;color:#eeeeee;font-family:sans-serif;">
			<div>
				Load Map: 
				<input type="file" id="map-file-input" accept=".json"/>
			</div>
			<div>
				Select State:
				<select id="state-selector-dropdown">
				</select>
				<button id="set-default-state-button">Set Default</button>
			</div>
		</div>
		{{/if}}
		{{#if project.app.web.live_reload '!==' false}}
		<script>document.write('<script src="http://' + (location.host || 'localhost').split(':')[0] + ':35729/livereload.js?snipver=1"></' + 'script>')</script>
		{{/if}}
        <script type="text/javascript" src="./{{project.app.name}}{{#if project.app.web.min '==' 'true'}}.min{{/if}}.js"></script>
    </body>

</html>
